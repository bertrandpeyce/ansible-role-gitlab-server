---
- hosts: all
  become: yes
  roles:
    - role: ansible-role-gitlab-server
      vars:
        domain_name: "mytestdomain.com"
    - role: postfix
      vars:
        domain_name: "mytestdomain.com"
